en:
  activerecord:
    models:
      tenant: Tenant
      user: User
    attributes:
      tenant:
        name: "name"
        host: "host"
      user:
        tenant: "tenant"
        email: "email"
        password: "password"
        password_confirmation: "password confirmation"
        name: "name"
  notice:
    invalid_tenant: "Invalid tenant"